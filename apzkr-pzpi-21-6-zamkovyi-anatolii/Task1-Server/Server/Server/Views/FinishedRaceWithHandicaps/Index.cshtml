@model IEnumerable<WebApplication3.Models.FinishedRaceWithHandicap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RaceWithHandicapId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinishTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RaceWithHandicapId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FinishTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FinishedRaceWithHandicapId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FinishedRaceWithHandicapId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FinishedRaceWithHandicapId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
