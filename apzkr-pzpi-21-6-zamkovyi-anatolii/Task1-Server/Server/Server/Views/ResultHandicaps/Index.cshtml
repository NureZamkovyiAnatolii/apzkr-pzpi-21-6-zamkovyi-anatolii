@model IEnumerable<WebApplication3.Models.ResultHandicap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CrewHandicapId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimeTaken)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CrewHandicapId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeTaken)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResultHandicapId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResultHandicapId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResultHandicapId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
